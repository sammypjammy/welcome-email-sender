Place fixed PDF attachments for welcome emails in this folder, then map each file to its case manager and language in `src/outlookConfig.js`.

Keep only mapped packets here so unused files are not copied into production builds.
