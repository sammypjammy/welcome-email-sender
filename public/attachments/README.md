Place the fixed PDF attachments for welcome emails in this folder.

After adding them, map each file to its case manager and language in `src/outlookConfig.js`.

The English and Spanish packets used by the app are mapped there. Unmapped packets remain in this folder but are not attached automatically.
